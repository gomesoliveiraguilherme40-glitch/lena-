import React from "react";
import { motion } from "framer-motion";

export default function SimpleLovePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-pink-300 via-red-300 to-purple-400">
      <motion.h1
        initial={{ opacity: 0, scale: 0.8 }}
        animate={{ opacity: 1, scale: 1 }}
        transition={{ duration: 1 }}
        className="text-white text-5xl sm:text-7xl font-extrabold text-center drop-shadow-lg"
      >
        Te amo Milena 💖
      </motion.h1>
    </div>
  );
}
